<UserControl x:Class="ShopCashes.CashControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ShopCashes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <!--<Label x:Name="number" Background="Blue"/>
        <WrapPanel x:Name="queue" Grid.Column="1" Background="LightBlue"/>-->
        <Label x:Name="number" FontSize="24"   
                           Width="60" Height="60"               
                           HorizontalContentAlignment="Center"
                           VerticalContentAlignment="Center"
                           Background="Blue" Foreground="White"
                           Content="{Binding Path=Id}"/>
        <ListBox x:Name="queue" Grid.Column="1" 
                           Background="LightBlue"/>

    </Grid>
</UserControl>
